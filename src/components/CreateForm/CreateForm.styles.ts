import styled from 'styled-components';

export const CreateFormContainer = styled.div`
    display: flex;
    width: 380px;
    position: relative;

    input {
        margin-right: 10px;
        width: 100%;
    }
`;
